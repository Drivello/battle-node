function shotRival(positions, xy){
    let result;
    
    for(let i in positions){
        if(positions[i].includes(xy)){
            if(xy.indexOf(1)){ //significa que es la ultima posicion del barco [0,1]
                ///
            }
        }
    }
}

